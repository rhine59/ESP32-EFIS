# Copy to .env on the Synology; never commit the real .env.
OTA_PUBLIC_BASE_URL=https://granvillehouse.synology.me:8448
# Generate with: openssl rand -hex 32
ADMIN_SESSION_SECRET=REPLACE_WITH_RANDOM_SECRET
