# EFIS Customer — iOS

Native SwiftUI customer companion, target iOS 17+. Initial skeleton uses mock data until authenticated account API contracts are frozen.

Planned secure storage: Keychain for account/session tokens. No card PAN/CVV storage. Checkout will use provider SDK/hosted flows (including Apple Pay where enabled).
